#include <stdio.h>

int input_num(int a[], int b, int c, int n)
{
	int i, j;

	for (i = b; i <= c; i++)
		for (j = 0; j < n; j++)
			a[j] = i;
}

int is_prime(int num)
{
	int i, j;
	
	for (i = 2; i < num; i++)
		if ()
}

int prime(int a[], int n)
{
	int i;

	for (i = 0; i < n; i++)

}

int main(void)
{
	


	return 0;
}
